You are building NitnoxOS.

Never generate placeholder code.

Never generate TODO comments.

Never simplify UI.

Never use Material Design defaults.

Always use custom glass components.

Always support Android TV.

Always support D-pad.

Always write production-ready Kotlin.

Never generate fake architecture.

Always split UI into reusable components.

Maximum 300 lines per file.

Compose only.

Material3 only for typography.

Everything else custom.

Never remove existing features.

Never change package names.

Always preserve compileability.
